[...목록으로 가기](./index.md)

# : : : [Module] TRANSACTION : : :

<br/>

## Action 함수목록
1. Init - 접속시 마지막 저장정보 로드하여 화면에 보여주는것까지 함
2. LoadSpace -  화면 로드 (파라미터 space.id)
3. MoveFront - 선택한 다이어그램 z-index를 맨 앞으로 가져오기
4. AddDiagram - 다이어그램 추가 (파라미터 type, x, y) 

## Render 함수목록
1. Draw - loop.draw 호출
2. Resize - loop.render 호출
3. zoom - loop.render 호출 

# 필요한거 정리해봐
- 다이어그램
    1. 생성
    2. 업데이트(이동, 에디터)
    3. 저장(DB까지)
    4. 조회
    5. 삭제
    6. 복사(ctrl+c -> ctrl+v 해서 붙여넣기)
- 맵
    1. 맵 로드(화면에 보이기 + 최근 연 맵 갱신 + 메멘토 목록 체크)
    2. 맵 이동(스페이스를 저장해야할지, 스페이스는 테이블을 따로 해야되나)
    3. 맵 내에 특정 다이어그램으로 화면이동(즐겨찾기)
    4. 다이어그램 검색해서 화면이동(타 스페이스로 이동도 가능하게)
- 사각형.Children
    1. 재귀형태로 붙여넣는다고 생각해보자
    2. 사각형에서 재귀형태로 넣은걸 어떻게 DB로 넘길껀지
    3. 그룹내에 독립된 id를 가진 다이어그램들은 또 어떻게
        그룹에서 관리할건지


# 트랜잭션에서 할필요 없어보이는거는
1. 다이어그램.사각형 에서 편집모드 켤 때 -> 에디터 직접호출
2. 컨트롤러에서 콜리전 체크 -> 로직 만들어봐

*사각형 콜리전 --> 이건 컨트롤러에서 할게 아니고 Loop로 완전 넘겨야될거같다*
*Loop에서 옵저버 형식으로 루프마다 체크한다 바람직하지 않지. 그럼 컨트롤러에서*
*매번 예약을 걸어주는건 나쁘진 않은데 매번 타겟을 넘겨주는건 컨트롤러에서 타겟이*
*루프보다 빨리 갱신되니까*
*1. 변수로 가지고 있는다: 루프에서 변수를 읽지만, 해당 시점에는 콜리전 범위 밖일 수 있어서 동시간에 콜리전 일어났을 수도 있으나 시간차로 체크값이 바뀌는 결과*
*2. 매번 파라미터로 건낸다: 컨트롤러에서 갱신한 최신 타겟과 달라서 의미 없지않나.*
*의미라고는 하나도 안빠지고 콜리전 체크는 할 수 있다. 근데 유저가 방문을 뚤고 겹쳐진 형태가 된다*
*3. Loop 안쓰고 컨트롤러 무브마다 호출해 비교: 정확하겠지만 연산량이 너무 많음*