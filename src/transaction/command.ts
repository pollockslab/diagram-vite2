
import { type _DT, _DC } from '../diagrams/diagrams.type'

export class _MAIN 
{
    constructor(type:string, x:number, y:number)
    {
        // 넣어야될거 보고 함수로 바꿀지 생각해보자고

        // 1. 파라미터( 다이어그램type, 마우스x, 마우스y, )


        // 2. 다이어그램 생성
        
        // 3. 다이어그램 초기화

        // 4. storage 에 저장

        // 5. view.Draw() 는 여기서 할게 아니지.
        console.log(type, x, y);
    }
}

// function AddSquare(type:_DT.CHILD_NAME, x:number, y:number)
// {
//     // 인단 만들어봐
//     console.log(type, x, y);

// }